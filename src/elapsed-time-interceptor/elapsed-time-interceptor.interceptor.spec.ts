import { ElapsedTimeInterceptorInterceptor } from './elapsed-time-interceptor.interceptor';

describe('ElapsedTimeInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ElapsedTimeInterceptorInterceptor()).toBeDefined();
  });
});
